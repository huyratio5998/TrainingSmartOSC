@model IEnumerable<MvcMusicStore.Models.Genre>

@{ 
    ViewBag.Title = "Store";
}
<h3>Browse Genres</h3>
<p>Select from @Model.Count() genres</p>
<ul>
    @foreach(var genner in Model)
    {
        <li>@Html.ActionLink(genner.Name,"Browse", new { genner=genner.Name})</li>
    }
</ul>
